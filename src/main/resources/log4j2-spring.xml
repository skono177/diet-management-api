<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
  <Properties>
    <Property name="logFilePath">${bundle:application:app.log.path}</Property>
    <Property name="logFileName">${bundle:application:app.log.name}</Property>
  </Properties>
  <Appenders>
    <!-- コンソール出力 -->
    <Console name="Console" target="SYSTEM_OUT">
      <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n" />
    </Console>

    <!-- ファイル出力 -->
    <File name="FileLogger" fileName="${logFilePath}/${logFileName}">
      <PatternLayout pattern="%d{yyyy-MM-dd HH:mm:ss} [%t] %-5level %logger{36} - %msg%n" />
    </File>
  </Appenders>

  <Loggers>
    <Root level="info">
      <AppenderRef ref="Console" />
      <AppenderRef ref="FileLogger" />
      <!-- <AppenderRef ref="RollingFileLogger"/> -->
    </Root>
  </Loggers>
</Configuration>